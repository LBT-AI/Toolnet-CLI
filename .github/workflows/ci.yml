name: ToolNet CLI CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    name: Typecheck, Test, Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: TypeScript
        run: bun run typecheck

      - name: Unit tests
        run: bun test

      - name: Build Bun and Node bundles
        run: bun run build

      - name: Bun smoke test
        run: bun dist/index.js --help

      - name: Node smoke test
        run: node dist/node/index.js --help

      - name: Node persistence smoke test
        run: |
          bun build scripts/node-persistence-smoke.ts --target=node --outfile=/tmp/toolnet-node-persistence-smoke.mjs
          node /tmp/toolnet-node-persistence-smoke.mjs

      - name: Launcher smoke test
        run: node bin/toolnet.js --version

      - name: NPM package dry run
        run: npm pack --dry-run
